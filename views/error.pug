extends layout

block content
  .main.container
    .row.text-xs-center
      .col-md-6.col-md-offset-3.p-t-2
        i.icn-person.material-icons error
        each message in errors
          p.lead.m-t-2= message.param +': '+ message.msg 
